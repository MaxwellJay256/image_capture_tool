# image capture tool

用 USB 摄像头拍照的小工具。

平台：Linux

## 运行

有两种方法：

1. `python image_capture_tool.py` 直接运行脚本
2. `pyinstalller -F image_capture_tool.py` 编译成可执行文件，然后运行 `dist/image_capture_tool/image_capture_tool`。
